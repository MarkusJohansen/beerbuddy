## What and why

<!-- What changes, and the problem it solves. -->

## Checks

- [ ] `make check` passes (lint, format, type check, unit and backend tests)
- [ ] Documentation updated if behaviour changed — README, ARCHITECTURE and the
      per-package READMEs all state concrete numbers and defects
- [ ] `git status` is clean after running the app

Ref #
