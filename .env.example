# Copy to .env (or run `make env`). Read by compose for the database service.

POSTGRES_USER=beerbuddy
POSTGRES_PASSWORD=beerbuddy
POSTGRES_DB=beers

# Production only — compose.prod.yaml refuses to start without a real value here.
# CORS_ORIGINS=http://localhost:8080
